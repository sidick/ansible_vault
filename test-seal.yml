---

- hosts: 127.0.0.1
  connection: local
  gather_facts: False
  become: False
  vars_files:
    - group_vars/vault

  tasks:
    - include: test-vault-credentials.yml

    - name: Seal the vault
      vault_seal:
        token: "{{ vault_token }}"
        state: sealed
        tls: off

    - name: Seal the vault
      vault_seal:
        token: "{{ vault_token }}"
        state: sealed
        tls: off
